{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M1 default capability — main window only, local content only, single custom command app::ping (Tauri invoke: ping). No shell, filesystem, http, process, opener, or remote capabilities.",
  "windows": ["main"],
  "permissions": ["allow-ping"]
}
